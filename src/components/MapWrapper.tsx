import MapView, { Marker, Polyline, Callout } from "react-native-maps";
import type { Region } from "react-native-maps";

export { MapView as default, Marker, Polyline, Callout };
export type { Region };
